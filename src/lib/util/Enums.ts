export enum Event {
	Raw = 'raw',
	Ready = 'ready',
	Message = 'message',
	Disconnect = 'disconnect'
}

export enum Statuses {
	online = 'online',
	offline = 'offline',
	idle = 'idle',
	dnd = 'do not disturb'
}
